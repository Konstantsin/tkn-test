# frozen_string_literal: true

RSpec.describe Tkn::Test do
  it "has a version number" do
    expect(Tkn::Test::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
